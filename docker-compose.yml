# docker-compose.yml
version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
  worker:
    build: .
    command: ["python", "-m", "app.worker"]
    depends_on:
      - redis
  redis:
    image: redis:7.2-alpine
    ports:
      - "6379:6379"

